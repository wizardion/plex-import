{
  "name": "plex-server",
  "version": "1.0.0",
  "description": "home-server for plex",
  "dependencies": {
    "plex-api": "^5.3.2",
    "yaml": "^2.1.1"
  },
  "devDependencies": {
    "jshint": "^2.13.4",
    "node-scp": "^0.0.17"
  },
  "scripts": {
    "predevstart": "npx jshint src/",
    "devstart": "node src/index.js",
    "deploy": "node src/deploy.js",
    "start": "node src/index.js --silent",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alex Zarnitsa",
  "license": "ISC"
}
